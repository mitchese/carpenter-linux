# Instance port — each parallel instance needs a unique host port
CARPENTER_PORT=7843

# Required: your Anthropic API key
ANTHROPIC_API_KEY=sk-ant-...

# Required: a secret token to protect the web UI
# Generate one with: python3 -c "import secrets; print(secrets.token_hex(16))"
UI_TOKEN=
